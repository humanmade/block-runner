# Split feature — utility/Tailwind source

Create the same `block-runner/split-feature` contract from the utility-class source. Preserve its responsive column behavior in the style ledger as generated CSS or block layout configuration, rather than shipping Tailwind classes.
