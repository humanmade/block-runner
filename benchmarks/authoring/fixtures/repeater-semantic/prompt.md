# Repeater — semantic source

Create `block-runner/customer-stories` from the ordered semantic list. Each story must be a repeatable child whose metric, quote, avatar, and citation remain editable. Verify add, reorder, save, and reload without changing the intended list semantics.
