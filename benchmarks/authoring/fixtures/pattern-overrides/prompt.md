# Pattern overrides

Materialize the supplied serialized core-block pattern as a `block-runner/signal-callout` pattern instance. Permit changes only to heading/body text, background, and button text/URL; preserve the core-block structure and report forbidden structural overrides.
