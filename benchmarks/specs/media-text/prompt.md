# Media + text (image beside text)

An image-beside-text feature row:
- One side: a dashboard image.
- Other side: an H2 "See everything in one place", a paragraph "A single view of conversions, traffic, and content health.", a bullet list ("Real-time metrics", "Custom segments"), and a button "Take the tour".

Output one self-contained HTML `<section>`. Semantic HTML; use your own CSS/classes.
