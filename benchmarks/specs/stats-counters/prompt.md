# Stats / counters

A stats band: an H2 "By the numbers", then a four-column row of figures. Each column is a big number and a label:
- "100k+" — "blocks converted"
- "99.9%" — "validation pass rate"
- "12ms" — "average per block"
- "0" — "API keys required"

Output one self-contained HTML `<section>`. Semantic HTML; use your own CSS/classes.
