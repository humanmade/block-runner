# File download section

A downloadable resource section:
- An H2 "Get the spec sheet".
- A paragraph "Every supported block, and what it maps from."
- A downloadable PDF link to `block-reference.pdf`, labelled "Block reference (PDF)", with a visible download button.

Output one self-contained HTML `<section>`. Semantic HTML; use your own CSS/classes.
