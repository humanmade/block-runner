# Video section

A product video section:
- An H2 "See it in motion".
- A paragraph "Ninety seconds from raw HTML to native blocks."
- A self-hosted video, file `product-demo.mp4`, with a poster image `demo-poster.jpg`, player controls visible.
- A caption beneath the video: "Converting a hero section, start to finish."

Output one self-contained HTML `<section>`. Semantic HTML; use your own CSS/classes.
