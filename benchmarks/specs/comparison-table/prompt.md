# Comparison table

A plan-comparison section: an H2 "Compare plans", then a table comparing a Free and a Pro
plan across three rows. Header row: Feature, Free, Pro. Rows:

- Seats — "1" / "Unlimited"
- Export — "No" / "Yes"
- Support — "Community" / "Priority"

Use a real HTML `<table>` for the tabular data. Output one self-contained HTML `<section>`.
Semantic HTML; use your own CSS/classes.
