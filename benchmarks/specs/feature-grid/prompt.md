# Feature grid (icon cards)

A features section: an H2 "Why teams choose us", then a three-column grid. Each column is an icon image, an H3, and a paragraph:
1. "Fast by default" — "Deterministic conversion with no manual cleanup."
2. "Always valid" — "Every output passes the editor's own validator."
3. "Open and scriptable" — "Run it in CI or straight from the command line."

Output one self-contained HTML `<section>`. Semantic HTML; use your own CSS/classes.
