# FAQ / accordion

An FAQ section: an H2 "Frequently asked questions", then three collapsible question/answer items:
1. "Does it work without a live WordPress site?" — "Yes. The gate runs headless Gutenberg in Node — no server required."
2. "Can I run it in CI?" — "Yes. It is a deterministic CLI with machine-readable JSON output."
3. "What about third-party blocks?" — "Supported through adapters, with tiered validation per block type."

Output one self-contained HTML `<section>`. Semantic HTML; use your own CSS/classes.
