# Logo cloud

A logo cloud: an eyebrow label "Trusted by teams at", then a horizontal strip of five client logo images (Acme, Globex, Initech, Umbrella, Northwind).

Output one self-contained HTML `<section>`. Semantic HTML; use your own CSS/classes.
