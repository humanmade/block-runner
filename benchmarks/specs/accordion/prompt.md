# Accordion section

A collapsible feature-detail section (a set of panels, only headings visible until opened):
- An H2 "How it works".
- Three collapsible panels, each with a heading and a paragraph of body text:
  1. "Read the design" — "Block Runner reads your markup and works out what each section means."
  2. "Assemble the blocks" — "Every block is built from verified primitives, so it is valid the moment it exists."
  3. "Prove it in the editor" — "The result is parsed and validated against real WordPress before you ever see it."

Output one self-contained HTML `<section>`. Semantic HTML; use your own CSS/classes.
