# Cards — semantic source

Create a `block-runner/card-collection` registered block with three editable cards. Card image, heading, body, and link are distinct fields. The editor must allow reordering cards without exposing raw serialized markup.
