# Split feature — semantic source

Create `block-runner/split-feature`. Its image, feature title, description, and three feature points must remain independently editable. On narrow viewports the text follows the image, preserving source reading order and landmark semantics.
