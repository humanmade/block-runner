# Local assets

Create `block-runner/asset-feature`. Bundle the local Aurora SVG with the plugin, emit a built asset URL rather than a network substitute or data URL, and surface the precise alternative text in the editor. The image and caption must survive a save/reload cycle.
