# Tailwind responsive CSS

Turn the utility source and adjacent responsive CSS into `block-runner/responsive-panel-grid`. The editor should offer understandable column/gap/padding controls while the generated frontend stylesheet retains the specified 1/2/4 column breakpoints and reduced-motion rule. The generated package must not require Tailwind.
