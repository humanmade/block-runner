# Hero — semantic source

Author a registered `block-runner/hero` block from the paired semantic source. The editor must expose eyebrow, heading, body, two calls to action, and product image/alt text as separate editable controls. Preserve the mobile stacked order: copy before image. Do not use a raw HTML fallback.
