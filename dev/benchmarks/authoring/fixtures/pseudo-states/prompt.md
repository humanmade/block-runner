# Pseudo states

Create `block-runner/style-callout` from the source and pseudo-state stylesheet. Record default, hover, and `:focus-visible` declarations in the style ledger. Only stable content/style controls are editor-exposed, but frontend CSS must retain keyboard-visible focus and the decorative pseudo-element.
