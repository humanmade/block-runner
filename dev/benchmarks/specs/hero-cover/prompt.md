# Hero (centered cover)

A full-width hero section with a background image and a two-column split:
- Left column: an image (a founder/portrait photo).
- Right column: an H2 "Built for handoff", a paragraph "Validate and canonicalize before publishing.", and a call-to-action button "Start now".

Output one self-contained HTML `<section>`. Semantic HTML; use your own CSS/classes.
