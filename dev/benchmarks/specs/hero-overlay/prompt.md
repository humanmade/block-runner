# Hero with overlay card

A full-width hero with a background image, and an overlaid content card centered on it. The
card contains, in order:

1. An H1 "Ship native blocks, not spaghetti".
2. A paragraph: "Convert any design handoff into clean, editor-valid WordPress blocks."
3. A two-column stat row:
   - "10k+" / "blocks converted"
   - "0" / "API keys required"
4. A button group with two buttons: "Get started" and "Read the docs".

The background image makes this a cover; the card is a grouped block over it. Output one
self-contained HTML `<section>`; semantic HTML, your own CSS/classes; placeholder image ok.
