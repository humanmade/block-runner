# Hero (split)

A full-width cover hero with a two-column split:
- Left column: an eyebrow line "New in 2026", an H1 "Ship design straight to native blocks", a paragraph "Turn any layout into clean, editable markup — no manual cleanup.", and a button group with two buttons: "Get started" and "Read the docs".
- Right column: the product screenshot image.

Output one self-contained HTML `<section>`. Semantic HTML; use your own CSS/classes.
