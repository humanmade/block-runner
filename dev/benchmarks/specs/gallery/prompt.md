# Gallery (screenshot grid)

A product gallery section: an H2 "See it in action", then a grid of three product
screenshots (a dashboard, the editor, and a report view). The images are decorative
screenshots, shown as a gallery — not a logo strip and not text columns.

Output one self-contained HTML `<section>`. Semantic HTML; use your own CSS/classes. You
may use placeholder image sources.
