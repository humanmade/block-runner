# Bento grid (mixed feature tiles)

A "One toolkit, three superpowers" feature section: an H2 "One toolkit, three superpowers",
then a row of three tiles — each tile is a DIFFERENT shape:

1. **Image-beside-text tile**: a screenshot (`panel.png`) on one side; on the other an H3
   "Deterministic" and a paragraph "The same input always yields the same blocks."
2. **Highlighted CTA tile** (a tile with its own background/overlay): an H3 "Editor-valid", a
   paragraph "Every block passes the editor's own gate.", and a button "See the gate".
3. **Plain card tile**: an icon image (`open.svg`), an H3 "Open", and a bullet list of "MIT
   licensed", "Scriptable CLI".

Make each tile visually distinct per its description. Output one self-contained HTML
`<section>`; semantic HTML, your own CSS/classes; placeholder images ok.
