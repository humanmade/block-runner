# CTA band

A full-width call-to-action band on a dark background: an H2 "Ready to ship cleaner blocks?", a paragraph "Start converting design HTML into native Gutenberg markup today — no manual cleanup, no lock-in.", and a single button "Get started free".

Output one self-contained HTML `<section>`. Semantic HTML; use your own CSS/classes.
