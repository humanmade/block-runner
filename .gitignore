# Internal agent context — private (may reference internal/dogfood projects).
# Never committed; the public repo must read clean.
AGENTS.md
CLAUDE.md

# Node
node_modules/
dist/
build/
*.tsbuildinfo
.env
.env.*
*.log

# OS / editor
.DS_Store
.vite/
.idea/
.vscode/
.clawpatch/

# generated benchmark reports
benchmarks/presentation/review.html
benchmarks/presentation/scoreboard.html
.playwright-mcp/

# tuner: cached propose artifacts + captured regressions (smoke.seed.json + baselines/ stay committed)
benchmarks/.cache/
benchmarks/regressions/

# Raw research reference (local)
WORDPRESS_BLOCK_PATTERNS_REFERENCE.md

# Internal context docs — never shipped
md/

# Demo: keep demo.gif (README hero); regenerate the heavier mp4 + hi-res gif from demo/demo.tape
demo/*.mp4
demo/demo.hq.gif
